import { r8, r9, r10, r11, r12, rm1, r13, r14, r15 } from "./recitalsImage.js"

// Heto na! Isang loop lang, gawa na ang 19 objects mo.
export const recitals8 = r8.map((img, index) => ({
    id: index + 1,
    image: img, // Ito yung kukuha sa r81, r82, etc.
    name: ''
}));

export const recitals9 = r9.map((img, index) => ({
    id: index + 1,
    image: img, 
    name: ''
}));

export const recitals10 = r10.map((img, index) => ({
    id: index + 1,
    image: img, 
    name: ''
}));

export const recitals11 = r11.map((img, index) => ({
    id: index + 1,
    image: img, 
    name: ''
}));

export const recitalsm1 = rm1.map((img, index) => ({
    id: index + 1,
    image: img, 
    name: ''
}));

export const recitals12 = r12.map((img, index) => ({
    id: index + 1,
    image: img, 
    name: ''
}));

export const recitals13 = r13.map((img, index) => ({
    id: index + 1,
    image: img, 
    name: ''
}));

export const recitals14 = r14.map((img, index) => ({
    id: index + 1,
    image: img, 
    name: ''
}));

export const recitals15 = r15.map((img, index) => ({
    id: index + 1,
    image: img, 
    name: ''
}));